class Session {
  /**
   * Constructor
   * @param {number} day Day number
   * @param {number} sessionLength Session duration
   */
  constructor(day, sessionLength) {
    this.day = day;
    this.sessionLength = sessionLength;
  }
}

export default Session;
